<UserControl x:Class="WPF_test.Pages.PLCToPCPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#607D8B" Padding="15,10">
            <Grid>
                <TextBlock Text="PLC ↔ PC 通讯" FontSize="20" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Content="✕" FontSize="20" Foreground="White" Background="Transparent" 
                        BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="PLC与PC通讯设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- 通讯设置 -->
            <GroupBox Grid.Row="1" Header="通讯参数" Margin="0,0,0,20">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="通讯协议：" FontSize="14" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Margin="10,5" SelectedIndex="0">
                        <ComboBoxItem>Modbus TCP</ComboBoxItem>
                        <ComboBoxItem>Modbus RTU</ComboBoxItem>
                        <ComboBoxItem>Profibus</ComboBoxItem>
                    </ComboBox>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="IP地址：" FontSize="14" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="10,5" Text="192.168.1.100"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="端口号：" FontSize="14" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="10,5" Text="502"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="通讯状态：" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Margin="10,5" Text="已连接" Foreground="Green" FontWeight="Bold"/>

                    <Button Grid.Row="4" Grid.Column="1" Content="连接测试" Width="100" Height="30" 
                            HorizontalAlignment="Left" Margin="10,10,0,0"
                            Background="#2196F3" Foreground="White" BorderThickness="0"/>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>