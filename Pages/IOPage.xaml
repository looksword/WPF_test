<UserControl x:Class="WPF_test.Pages.IOPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#607D8B" Padding="15,10">
            <Grid>
                <TextBlock Text="IO页面" FontSize="20" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Content="✕" FontSize="20" Foreground="White" Background="Transparent" 
                        BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="IO输入输出状态" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- 输入IO -->
            <GroupBox Grid.Row="1" Grid.Column="0" Header="输入IO" Margin="10">
                <StackPanel Margin="10">
                    <CheckBox Content="I0.0 - 启动按钮" Margin="5"/>
                    <CheckBox Content="I0.1 - 停止按钮" Margin="5"/>
                    <CheckBox Content="I0.2 - 复位按钮" Margin="5"/>
                    <CheckBox Content="I0.3 - 急停按钮" Margin="5"/>
                    <CheckBox Content="I1.0 - 传感器1" Margin="5"/>
                    <CheckBox Content="I1.1 - 传感器2" Margin="5"/>
                </StackPanel>
            </GroupBox>

            <!-- 输出IO -->
            <GroupBox Grid.Row="1" Grid.Column="1" Header="输出IO" Margin="10">
                <StackPanel Margin="10">
                    <CheckBox Content="Q0.0 - 电机1" Margin="5"/>
                    <CheckBox Content="Q0.1 - 电机2" Margin="5"/>
                    <CheckBox Content="Q0.2 - 气缸1" Margin="5"/>
                    <CheckBox Content="Q0.3 - 气缸2" Margin="5"/>
                    <CheckBox Content="Q1.0 - 指示灯1" Margin="5"/>
                    <CheckBox Content="Q1.1 - 指示灯2" Margin="5"/>
                </StackPanel>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>