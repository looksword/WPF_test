<UserControl x:Class="WPF_test.Pages.PLCToRobotPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#607D8B" Padding="15,10">
            <Grid>
                <TextBlock Text="PLC ↔ ROBOT 通讯" FontSize="20" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Content="✕" FontSize="20" Foreground="White" Background="Transparent" 
                        BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="PLC与机器人通讯设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- 机器人通讯设置 -->
            <GroupBox Grid.Row="1" Header="机器人参数" Margin="0,0,0,20">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="机器人型号：" FontSize="14" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Margin="10,5" SelectedIndex="0">
                        <ComboBoxItem>ABB IRB 120</ComboBoxItem>
                        <ComboBoxItem>KUKA KR 6</ComboBoxItem>
                        <ComboBoxItem>Fanuc LR Mate</ComboBoxItem>
                    </ComboBox>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="通讯方式：" FontSize="14" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="10,5" SelectedIndex="0">
                        <ComboBoxItem>以太网</ComboBoxItem>
                        <ComboBoxItem>Profibus</ComboBoxItem>
                        <ComboBoxItem>DeviceNet</ComboBoxItem>
                    </ComboBox>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="IP地址：" FontSize="14" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="10,5" Text="192.168.1.200"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="连接状态：" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Margin="10,5" Text="已连接" Foreground="Green" FontWeight="Bold"/>

                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="10,10,0,0">
                        <Button Content="连接机器人" Width="120" Height="30" Margin="0,0,10,0"
                                Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                        <Button Content="断开连接" Width="120" Height="30"
                                Background="#F44336" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>