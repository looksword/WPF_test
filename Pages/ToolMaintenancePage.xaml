<UserControl x:Class="WPF_test.Pages.ToolMaintenancePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#607D8B" Padding="15,10">
            <Grid>
                <TextBlock Text="刀架刀具规格维护" FontSize="20" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Content="✕" FontSize="20" Foreground="White" Background="Transparent" 
                        BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="刀具规格管理" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- 刀具列表 -->
            <DataGrid Grid.Row="1" AutoGenerateColumns="False" Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="刀具编号" Width="100" Binding="{Binding ToolId}"/>
                    <DataGridTextColumn Header="刀具名称" Width="150" Binding="{Binding ToolName}"/>
                    <DataGridTextColumn Header="规格型号" Width="150" Binding="{Binding Model}"/>
                    <DataGridTextColumn Header="直径(mm)" Width="100" Binding="{Binding Diameter}"/>
                    <DataGridTextColumn Header="长度(mm)" Width="100" Binding="{Binding Length}"/>
                    <DataGridTextColumn Header="状态" Width="100" Binding="{Binding Status}"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="添加刀具" Width="100" Height="35" Margin="5" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                <Button Content="编辑刀具" Width="100" Height="35" Margin="5" Background="#FF9800" Foreground="White" BorderThickness="0"/>
                <Button Content="删除刀具" Width="100" Height="35" Margin="5" Background="#F44336" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>