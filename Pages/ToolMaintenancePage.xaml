<UserControl x:Class="WPF_test.Pages.ToolMaintenancePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- 左侧表格区域 -->
            <ColumnDefinition Width="Auto" />
            <!-- 右侧按钮区域 -->
        </Grid.ColumnDefinitions>

        <!-- 左侧区域：两个表格左右排列 -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 第一个表格：刀架 Q01~Q20 -->
            <DataGrid Grid.Column="0"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding ToolsFirstGroup}"
                      Margin="0,0,5,0"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      RowHeight="NaN">
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="Auto" />
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="刀架编号" Width="1*" Binding="{Binding ToolPostId}" IsReadOnly="True" />
                    <DataGridTemplateColumn Header="刀具信息" Width="3*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding ToolInfo, UpdateSourceTrigger=PropertyChanged}"
                                         BorderThickness="0"
                                         Background="Transparent"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- 第二个表格：刀架 Q21~Q40 -->
            <DataGrid Grid.Column="1"
                      Height="Auto"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding ToolsSecondGroup}"
                      Margin="5,0,0,0"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      RowHeight="NaN">
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="Auto" />
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="刀架编号" Width="1*" Binding="{Binding ToolPostId}" IsReadOnly="True" />
                    <DataGridTemplateColumn Header="刀具信息" Width="3*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding ToolInfo, UpdateSourceTrigger=PropertyChanged}"
                                         BorderThickness="0"
                                         Background="Transparent"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- 右侧区域：返回按钮（底部对齐） -->
        <Grid Grid.Column="1" Margin="10">
            <Button Content="返回"
                    Width="80"
                    Height="35"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Center"
                    Background="#607D8B"
                    Foreground="White"
                    BorderThickness="0"
                    Click="BackButton_Click" />
        </Grid>
    </Grid>
</UserControl>