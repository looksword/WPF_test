<UserControl x:Class="WPF_test.Pages.FunctionRotatePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF9800" Padding="15,10">
            <Grid>
                <TextBlock Text="功能旋转" FontSize="20" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Content="✕" FontSize="20" Foreground="White" Background="Transparent" 
                        BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="功能旋转控制" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- 旋转控制 -->
            <GroupBox Grid.Row="1" Header="旋转控制" Margin="0,0,0,20">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="旋转角度：" FontSize="14" VerticalAlignment="Center" Margin="5"/>
                    <Slider Grid.Row="0" Grid.Column="1" Margin="5" Minimum="0" Maximum="360" Value="90" TickFrequency="45"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="90°" FontSize="14" VerticalAlignment="Center" Margin="5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="旋转速度：" FontSize="14" VerticalAlignment="Center" Margin="5"/>
                    <Slider Grid.Row="1" Grid.Column="1" Margin="5" Minimum="10" Maximum="100" Value="50"/>
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="50%" FontSize="14" VerticalAlignment="Center" Margin="5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="旋转方向：" FontSize="14" VerticalAlignment="Center" Margin="5"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" Margin="5" SelectedIndex="0">
                        <ComboBoxItem>顺时针</ComboBoxItem>
                        <ComboBoxItem>逆时针</ComboBoxItem>
                    </ComboBox>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="当前位置：" FontSize="14" VerticalAlignment="Center" Margin="5"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Margin="5" Text="45°" FontWeight="Bold" Foreground="Blue"/>

                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="5,10,0,0">
                        <Button Content="开始旋转" Width="120" Height="35" Margin="0,0,10,0"
                                Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                        <Button Content="停止旋转" Width="120" Height="35" Margin="0,0,10,0"
                                Background="#F44336" Foreground="White" BorderThickness="0"/>
                        <Button Content="复位" Width="120" Height="35"
                                Background="#FF9800" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>