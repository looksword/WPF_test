<UserControl x:Class="WPF_test.Pages.ProcessMonitorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#607D8B" Padding="15,10">
            <Grid>
                <TextBlock Text="流程步监控" FontSize="20" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Content="✕" FontSize="20" Foreground="White" Background="Transparent" 
                        BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- 内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <StackPanel>
                <TextBlock Text="流程步监控界面" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                <!-- 流程步骤列表 -->
                <ListBox Height="300" Margin="0,0,0,20">
                    <ListBoxItem>步骤1：初始化系统</ListBoxItem>
                    <ListBoxItem>步骤2：加载配置文件</ListBoxItem>
                    <ListBoxItem>步骤3：连接设备</ListBoxItem>
                    <ListBoxItem>步骤4：启动服务</ListBoxItem>
                    <ListBoxItem>步骤5：运行监控</ListBoxItem>
                </ListBox>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>